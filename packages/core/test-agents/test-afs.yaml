type: ai
afs:
  modules:
    - module: history
      options:
        storage:
          url: ":memory:"
    - module: local-fs
      mountPoint: /working-dir
      options:
        localPath: ./afs-local-fs-storage
        description: "Local file system storage for AFS example"
