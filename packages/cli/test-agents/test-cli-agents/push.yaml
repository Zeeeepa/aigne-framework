type: ai
name: push
description: Push components to remote repository
input_schema:
  type: object
  properties:
    url:
      type: string
      description: URL of the remote repository
    component_name:
      type: string
      description: Name of the component to push
  required: ["component_name", "version"]
